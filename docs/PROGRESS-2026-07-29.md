# Progress Log — Survivor Game Correction Plan (2026-07-29)

Spec: `docs/TASK-correction-plan.md` · Findings: `docs/REVIEW-2026-07-29.md`
All Python runs use `.venv/bin/python`.

---

- [x] 20:02 **Step 0 — git repair.** Corrupt packfile (`pack-bd27a5bb…` truncated) made `git fsck` fail.
  Fresh-cloned `origin/main` to scratchpad, verified `git fsck` clean, installed the clean `.git`
  over the preserved working tree, re-verified `git fsck` clean. Working tree was 1 unpushed commit
  ahead (`829be4d` Swift 6 concurrency fixes) — that commit object is gone but its **content is
  preserved verbatim** in baseline commit `9ab4a30`. Corrupt `.git` parked at
  `../_survivor-git-corrupt-backup/` (12 MB) — safe for Tyler to delete once satisfied.
  Added `ios/SourcePackages/` to `.gitignore`. Commit `9ab4a30`.

- [x] 20:41 **F1 — Character-card lives.** Vote-out decrements `characterCards` (2→1→0);
  `isEliminated` + Jury membership + Inheritance only fire at 0. Handles single/double/tie-break
  eliminations through one choke point (`complete_tribal`). Final Tribal still triggers at 2
  players remaining regardless of cards left; added a 1-player-left safety that ends the game.
  Verified live: Bob voted out → lives 1, still in game, jury empty. Commit `2b09345`.
- [x] 20:41 **F2 — Vote-card economy.** Setup: 3 Action Cards + exactly 1 Vote Card each, and
  `create_deck` no longer puts Vote Cards in the Draw Pile (3p official deck = 47 cards).
  `cast_vote` spends real cards from hand — Vote/Goodwill Gamble are mandatory, Extra Vote
  optional; a player whose Vote Card was stolen can legally pass. 1 Vote Card returns to every
  player still in the game after each tribal. `extraVotes` is now derived from actual cards
  (`sync_vote_counters`). Card texts corrected against the Survival Guide:
  **Control The Vote** takes a physical Vote Card (it was wrongly reassigning the Council
  Leader — that's "I'm The Leader Now"), **Goodwill Gamble** hands the card to the target as a
  mandatory vote, **Extra Vote** is spent at voting time instead of incrementing a phantom
  counter. Commit `2b09345`.
- [x] 20:41 **F5 — Steal→Play→Draw enforced.** `draw_card` consults
  `rules_engine.get_current_turn_phase` and refuses with a clear message if the player hasn't
  stolen; also refuses while a reactive-card window is open. Verified live.
- [x] 20:41 **F6 — Heartbeat.** `on_heartbeat(data=None)` accepts the client's `{t}` payload and
  returns an ack dict so the client's RTT measurement resolves.
- [x] 20:41 **F7 — Deck mode toggle.** `deckMode: "official" | "extended"` (default official) +
  `expansion` flag on game creation, wired through `create_deck`. Official = 67 cards, extended
  adds the 7 house cards. `survivor_cards.json` metadata rewritten to stop claiming the 74-card
  deck is official; the 7 house cards are flagged `"official": false`. Verified tribal table for
  all player counts: 3p=4S/0D, 4p=2S/2D, 5p=2S/3D, 6p=0S/5D, bottom card always tribal.
- [x] 20:41 **F9 — Tie-break cascade.** `SurvivorRulesEngine.resolve_tribal_eliminations()` — a
  pure, testable function implementing the official double-elim cascade and the
  "unclear who is voted out" priority ladder. `tie_break` accepts incremental multi-picks;
  `enhanced_tie_break` is no longer a stub.
- [x] 20:41 **F10 — Official final-tribal questions** now used verbatim.
- [x] 20:41 **Phase 4 foundation.** `challenges.py` — the 4 digitally-playable Rocks Challenges
  plus the Immunity Idol Necklace framework. All 4 smoke-tested to a winner at 3/4/6 players.
  Hide 'n' Seek is a documented stub.

### Notes / decisions worth Tyler's attention

1. **The task brief's challenge setup numbers were swapped.** The official Challenge Survival
   Guide says **Highest Bidder = 10 grey + 1 purple** (no player-count table) and
   **Pull or Steal = 1 purple + grey per table (3p→2, 4p→3, 5p→4, 6p→5)**, i.e. exactly one rock
   per player. The brief had these the other way round. Implemented per the PDF.
2. **Eliminated players and Challenges — the brief and the PDF disagree.** The brief says
   eliminated players still take part. That is the **standalone** rule. The **combined-mode**
   guide is explicit: *"If both of your Survivor Character Cards have been voted out you can't
   take part in Challenges."* Since we're implementing combined mode, the PDF wins — eliminated
   players are excluded from Challenge participation.
3. **"deck of 76 Action Cards".** The combined-mode instructions reference adding the Orange
   cards to "the deck of 76 Action Cards", while the rules sheet in `docs/survivor_rules.md`
   says 67. Newer printings may differ. We build from whatever `survivor_cards.json` declares,
   so this needs no code change — worth checking against a physical copy.
4. **Necklace wearer treated as un-votable AND last-resort for the leader's tie-break pick.**
   The expansion says players "can't vote for you"; the standalone sheet says the wearer is
   "safe". So `cast_vote` rejects votes for the wearer, and the tie-break ladder only reaches
   them in tier 3 alongside Immunity Idol players.

- [x] 21:25 **F3 — State resync fixed and verified live.** Two bugs, not one.
  (a) `state-manager.js:436` called the GET-only route with `apiCall`'s default POST.
  (b) **New finding the review missed:** the service worker's `isStaticAsset()` tested
  `url.href.includes(asset)` against a `STATIC_ASSETS` list containing `'/'` — which matches
  *every* request. So `/api/game/<id>/state` was served **cache-first from the SW cache**, and a
  reconnecting phone got a permanently stale board even once the verb was fixed. Now: API
  requests route first, `isStaticAsset` matches by exact pathname (exact href for the CDN entry),
  the SW fetches API calls with `cache:'no-store'`, and the state route sends real `no-store`
  headers instead of `no-cache` + a per-second ETag. Verified in Chrome: plain `fetch` through
  the SW reflects server mutations immediately (deck 52→51 seen on the next plain fetch).
  Also bumped SW cache names to v2.0.0 so installed PWAs pick up the new JS/CSS. Commit `9b93280`.
- [x] 21:25 **F4 — HTTPS wedge removed.** The inline redirect is gone (with a comment explaining
  why it must not come back). Verified: `location.replace` occurrences in the served HTML = 0,
  page loads clean with 0 console errors.
- [x] 21:25 **F1 UI — lives display.** 🔥🔥 / 🔥🕯️ / 💀 in the lobby player list plus a new lives
  strip on the playing screen. Verified in Chrome: `Alice 🔥🔥 | Bob 🔥🕯️ | Cara 🔥🔥`.
- [x] 21:25 **F7 UI — deck toggles.** Deck select (official 67 / extended 74) + "Add Let's Go To
  Rocks Challenge Cards" checkbox on the create-game screen, plumbed through `createGame`.
  Verified live: create with `{deckMode:'official', expansion:true}` yields a 52-card 3p deck
  containing all 5 Orange cards.
- [x] 21:25 **Phase 4 UI — challenge panel.** Renders the active Challenge with controls driven by
  the server's `actions` list (so the UI can't offer an illegal move), per-challenge scoreboards,
  a 📿 badge for the Necklace wearer, and necklace holders excluded from vote targets. Verified
  in Chrome for a Highest Bidder state, and necklace exclusion verified
  (`[Cara, Bob]` → `[Bob]` once Cara wears it).
- [x] 21:25 **Bonus robustness:** the global Flask exception handler was turning every 404/405
  into a 500 ("internal server error" toasts for what were really routing mistakes). HTTP
  exceptions now pass through with their real status.

5. **Secret challenge information is stripped server-side.** `get_game_state` deletes any
   `_`-prefixed keys from the challenge (secret rock pulls) before the state leaves the server,
   so Lowest Score Loses and Pull or Steal are genuinely hidden until the reveal. Verified in
   Chrome (`'_rocks' in challenge === false`). Note the pre-existing design already ships every
   player's *hand* to every client — that's unchanged here, but worth a future look.
